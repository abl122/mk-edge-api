meta {
  name: Chamado Específico
  type: http
  seq: 3
}

get {
  url: {{baseUrl}}/request/123/suporte?tenant_id={{tenantId}}
  body: none
  auth: bearer
}

params:query {
  tenant_id: {{tenantId}}
}

auth:bearer {
  token: {{authToken}}
}

docs {
  Busca um chamado específico (formato legado).
  
  Path params:
  - id: ID do chamado
  - request_type: Tipo (suporte, instalacao, etc)
  
  Requer autenticação.
}
