meta {
  name: Login Cliente
  type: http
  seq: 1
}

post {
  url: {{baseUrl}}/sessions?tenant_id={{tenantId}}
  body: json
  auth: none
}

params:query {
  tenant_id: {{tenantId}}
}

body:json {
  {
    "login": "brito",
    "password": "F@lcon2931"
  }
}

docs {
  Faz login de um cliente usando login e senha.
  
  Body:
  - login: Login do cliente
  - password: Senha do cliente
  
  Retorna token JWT para usar nas requisições autenticadas.
}
