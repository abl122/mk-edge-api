meta {
  name: Test Agente
  type: http
  seq: 2
}

get {
  url: {{baseUrl}}/agent/test?tenant_id={{tenantId}}
  body: none
  auth: none
}

params:query {
  tenant_id: {{tenantId}}
}

docs {
  Executa uma query simples (SELECT 1) no agente para testar conectividade.
  
  Retorna:
  - ok: true/false
  - result: resultado da query
}
