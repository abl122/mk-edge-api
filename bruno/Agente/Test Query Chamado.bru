meta {
  name: Test Tabela Mensagens
  type: http
  seq: 5
}

post {
  url: {{baseUrl}}/agent/test?tenant_id={{tenantId}}
  body: json
  auth: bearer
}

params:query {
  tenant_id: {{tenantId}}
}

auth:bearer {
  token: {{authToken}}
}

body:json {
  {
    "sql": "SHOW COLUMNS FROM sis_msg"
  }
}

docs {
  Tabela de mensagens/notas é: sis_msg
  
  Campos:
  - chamado: STRING - ID do chamado
  - msg: TEXT - Conteúdo da mensagem
  - atendente: STRING - Nome do atendente
  - msg_data: DATE - Data da mensagem
  - login: STRING - Login do usuário
  - tipo: STRING - Tipo da mensagem
  1. sis_suporte_msgs
  2. sis_mensagens
  3. sis_suporte_mensagens
  4. Outra?
  
  Se retornar erro "Table not found", tente a próxima opção.
}
